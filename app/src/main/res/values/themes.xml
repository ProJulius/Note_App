<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Note_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Note_App" parent="Base.Theme.Note_App" />

    <style name="YourCustomDialogFragmentStyle" parent="android:Theme.Dialog">

        <item name="android:windowBackground">@drawable/background_dialog</item>
        <!-- Other style attributes for your dialog fragment -->
    </style>

    <style name="DefaultDateStyle">
        <item name="android:textColor">@android:color/black</item> <!-- Màu văn bản -->
        <!-- Các thuộc tính khác của văn bản như textSize, textStyle, fontFamily, ... -->
    </style>
</resources>